backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: static/images/news
public_folder: /images/news
collections:
  - name: 'news'
    label: 'News'
    folder: 'content/news'
    create: true
    slug: '{{slug}}'
    preview_path: 'news/{{slug}}'
    editor:
      preview: true
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { label: 'Author', name: 'author', widget: 'hidden', default: 'Hannah & Will' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
      - { label: 'type', name: 'type', widget: 'hidden', default: 'posts'}
      - { label: 'Categories', name: 'categories', widget: 'list',  default: ['News']}
      - { label: 'Tags', name: 'tags', widget: 'list' }
      - { label: 'Featured Image', name: 'image', widget: 'image', required: TRUE}